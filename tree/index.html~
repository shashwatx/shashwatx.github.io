---
title: Maria's Recipes - Kitchen Tree
---
<div class="container">
</div>

<div class="project-container">
	<h1>Recipe Gallery</h1>
    <p>An attempt to generate collapsible, radial, Reignold-Tilford tree for easy navigation of recipes </p>
	<br><center>
</div>

<div class="container">

	<div class="project-box">


			{% for post in site.posts %}
                {% if post.type == "" or post.type == nil %}
                    {% if post.spice == "" or post.spice == nil %}
                        <div class="row row2">
                                <div class="col-md-3 project-image">
                                    <img src="{{post.eyecatcher}}">
                                </div>
                                <div style="clear:left;" class="col-md-9 project-post">
                                    <h3>{{ post.title }}</h3><h4><a href="{{post.url}}">Leer mas</a></h4>
                                </div>
                        </div>
                    {% endif %}
                {% endif %}
			{% endfor %}

	</div>
</div>
</div>
